// Ubah contoh checkout sehingga getProducts() mem-fetch tiga sumber produk paralel, lalu gabungkan hasilnya.
function getProducts() {
    
}
// Buat fetchWithTimeout(url, ms) yang menggunakan Promise.race + fetch.

// Buat fungsi retryFetch(url, opts) yang melakukan 3 retrydengan exponential backoff.